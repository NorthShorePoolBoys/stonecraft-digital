

# Make Header Logo Much Bigger

## Changes

### `src/components/Header.tsx` (line 9)
- Change logo from `h-20 md:h-24` to `h-32 md:h-40` — this makes it roughly 2x the current size, producing a prominent header logo

